# Dependencias para el análisis (notebooks/) — incluye el runtime
-r requirements.txt

# Entorno de notebooks
jupyter>=1.0

# Análisis de series de tiempo y estadística
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3
# Prophet (Video 13). En Windows con locale español, el notebook agrega tbb.dll al
# PATH automáticamente para evitar un bug de carga del backend de Stan.
prophet>=1.1

# Visualización
matplotlib>=3.8
plotly>=5.20
plotly-resampler>=0.9

# Calidad de código y tests
pytest>=8.0
black>=24.0
pre-commit>=3.7
nbstripout>=0.7
